#include<stdio.h>
int main()
{       int num,i,rem,sum=0,num1;
        printf("Enter a number:");
        scanf("%d",&num);
        num1=num;
        while(num>0)
        {
                rem=num%10;
		suy
        }

        return 0;
}
~       
